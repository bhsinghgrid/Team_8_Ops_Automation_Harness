# All necessary configurations are now managed via environment variables
# in the docker-compose.yml file. This keeps the setup clean and centralized.

# This setting resolves the "Cannot find template location" warning.
spring.mustache.check-template-location=false
